<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="XamlInteg.Views.PrismHomePage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:button="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    xmlns:prismNavi="clr-namespace:Prism.Navigation.Xaml;assembly=Prism.Forms"
    Title="{Binding Title}"
    prism:ViewModelLocator.AutowireViewModel="True">
    <StackLayout>
        <Button
            Command="{Binding ButtonCommand}"
            CommandParameter="property_changed"
            Text="Prism Property Changed" />
        <button:SfButton
            Command="{prismNavi:NavigateTo 'PrismLifecyclePage'}"
            CommandParameter="TEST"
            Text="Prism Lifecycle" />
        <button:SfButton Command="{prismNavi:NavigateTo 'PrismEventToCommandBehaviorPage'}" Text="EventToCommandBehavior" />
    </StackLayout>
</ContentPage>