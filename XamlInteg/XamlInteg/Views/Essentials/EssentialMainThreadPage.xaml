<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="XamlInteg.Views.EssentialMainThreadPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    prism:ViewModelLocator.AutowireViewModel="True">
    <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <Label x:Name="Label_Countdown" Text="{Binding Countdown}" />
        <Button Command="{Binding StartCommand}" Text="Start" />
        <ActivityIndicator x:Name="ActivityIndicator_IsBusy" IsRunning="{Binding IsBusy}" />
    </StackLayout>
</ContentPage>